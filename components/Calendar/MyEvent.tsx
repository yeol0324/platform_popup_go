export default interface MyEvent {
  id: string;
  title: string;
  start: Date;
  end: Date;
  location: string;
  info_url: string;
  image_url: string;
  reservate_url: string;
  isAllDay: boolean;
}
